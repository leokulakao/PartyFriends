import { Text } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

export default function NoCameraView() {
  return (
    <SafeAreaView>
      <Text>No Camera View</Text>
    </SafeAreaView>
  );
}
